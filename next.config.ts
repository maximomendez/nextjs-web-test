import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  //Just allowed in "Canary Versions"
  // experimental: {
  //   ppr: 'incremental', //Enable (Partial Prerendering) Allow to combine static and renderings components)
  // },
};

export default nextConfig;
